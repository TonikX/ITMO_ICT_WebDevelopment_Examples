/**
 * Типы в js
 */

let integerNumber = 3;  // number
let floatNumber = 3.14; // number

let bigInteger = 9007199254740991 // BigInt -(253-1) - (2^53-1)
let str = "Hello world"; // String
let isValid = true; // Bool
let nullVall = null; // Null ссылкой на несуществующий объект / нулевой указатель

let objectVal = {};  // Object
let arrayVal = [ 0.55, 0.32, 0.91 ];   // Object

let funVal = function () {}; // Function

// специальные типы
let inf = 1 / 0;        // Infinity
let notANumber = "string" / 0; // NaN
let undefinedVal = undefined; // undefined значение не было присвоено
